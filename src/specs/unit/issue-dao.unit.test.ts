import IssueDao from '../dao/issue-dao-impl';

describe('IssueDao Unit Test Suite', () => {
    it('Get Next Issue', () => {
        /* const nextKey = new IssueDao().getNextKey();
        expect(nextKey).toBe('TEST-2'); */
    })
})